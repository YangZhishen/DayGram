<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android">
    <alpha android:interpolator="@android:anim/accelerate_interpolator"
        android:duration="270"
        android:zAdjustment="top"
        android:fromAlpha="1"
        android:toAlpha="0.3"/>

    <!--
    控制透明度的动画效果：alpha
        浮点型值：
            fromAlpha：为动画起始时的透明度
            toAlpha：为动画结束时的透明度
            说明：
                0.0：表示完全透明
                1.0：表示完全不透明
            以上值取为 0.0 - 1.0 之间的 float 类型数字
        长整型值：
            duration：为动画的持续时间
            说明：
                时间以毫秒为单位
                accelerate_interpolator：加速动画插入器
                android:zAdjustment="top"：在动画播放期间，强制把当前播放的内容放到其他内容的上面
    -->
</set>